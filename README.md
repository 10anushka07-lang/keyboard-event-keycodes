 listens to keyboard 
input and displays key details in real time using the KeyboardEvent object.It shows:<br> 
• event.key → Logical key value (A, Enter, Shift) <br>
• event.keyCode → Numeric key code (deprecated, but useful for legacy understanding) <br>
• event.code → Physical key position (ShiftLeft / ShiftRight) <br>
